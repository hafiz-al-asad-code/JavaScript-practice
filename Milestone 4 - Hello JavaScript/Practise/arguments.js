function difference(number1, number2){
  const diff = number1 - number2;
  console.log(diff);
}

let fatherAge = 60;
let sonAge = 24;
difference(fatherAge, sonAge);

let motherAge = 60; daughterAge = 18;
difference(motherAge, daughterAge);