/*
Take four parameters. Multiply the four numbers and then return the result
*/

function multifly(number1, number2, number3, number4){
  let result = number1 * number2 * number3 * number4;

  return result;
}

let multification = multifly(3, 5, 10, 3);

console.log(multification);